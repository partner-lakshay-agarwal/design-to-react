# Copy this file to `.env` and fill in the value(s) you need.
# Only required for FIGMA mode. Image mode needs nothing here.
#
# Generate a token at: figma.com -> Settings -> Security -> Personal access tokens
# NEVER commit your real .env or paste the token into chat.

FIGMA_TOKEN=

# Alternative name also accepted by the extractor:
# FIGMA_API_KEY=
